---
description: Project playbook — required reading, folder placement, CADS styling, and doc dispatch for every task
alwaysApply: true
---

# Lab2 Prototype Kit

Follow `AGENTS.md` at the repository root before writing any code.

Skills live in `.claude/skills/` (Claude Code and Cursor both load that path): `add-experiment`, `add-lab-route`, `prepare-commits`.
